# M11-T12 术语解释验收

范围：模型参数弹窗中的识别置信度、IoU 去重阈值、单帧最大结果数。

- 信息按钮与输入标签分离，保留 label/for 关联；按钮使用 aria-describedby 关联真实 tooltip 内容。
- mouseenter 和 focus 展开，点击固定，再次点击、点击外部、Esc 关闭；Esc 关闭提示时不关闭参数弹窗。
- 置信度解释为模型评分，不将其等同于实际正确率；IoU 解释交并比和去重影响；最大结果数解释单帧保留上限。
- Live API 的 helmet 模型浏览器验证：三个解释文字可读；Tab 可显示 IoU 解释；Esc 后提示消失而 dialog 仍 open；再次点击和点击输入框均关闭提示。
- 390×844 视口检查：提示在弹窗左右边界内完整显示，当前参数输入框位于提示下方；完成后恢复默认桌面视口。
- 交互前后输入值为 0.35、0.45、100，未保存模型配置。

验证：node --check app/static/app.js；后台页面回归 23/23；Harness 单测 30/30；产品文件 git diff --check 通过。

限制：触屏行为通过浏览器点击路径与窄屏布局验证，未在触屏硬件上实测。参数同步与告警投递范围保持原任务边界。
