# Calibration data mount

Place the INT8 calibration `dataset.yaml` and its referenced images in this
directory on the deployment host. Dataset contents are intentionally ignored by
Git and excluded from Docker build contexts.
