# 服务端视频流分析服务：项目定位与需求基线

文档版本：V2.0（按 [ADR-002](docs/adr-002-production-transport.md) 重构输出传输协议）
适用范围：`server-video-service` 独立服务，不包含、不修改 `mobile-app/`
状态：需求基线

> V1.0 把 MJPEG/WebSocket JPEG 作为 MVP 与 P1 的输出协议。ADR-002 已把生产输出协议改为 **WebRTC/WHEP 主链路 + LL-HLS 回退 + RTSP 诊断**，MJPEG/WebSocket JPEG 降级为测试与诊断通道。本版按该决策重写第 1、2、3（FR-04/FR-08）、4、5、7、9 章。

## 1. 项目定位

### 1.1 一句话定位

这是一个**面向移动巡检 App 的服务端视频分析网关**：接收移动端或摄像头的视频流，在服务器上按需执行 YOLO 检测，把结果编码为一路 H.264 并通过媒体服务器分发给多个观看者，同时把结构化检测结果以独立通道下发。

### 1.2 要解决的问题

当前移动端视频流分析会持续占用手机 CPU/GPU/NPU、电量和散热余量，长时间运行时容易出现帧率下降、发热、后台限制和模型包体维护成本高等问题。本服务把“视频流的接收、解码、推理、叠加、编码、分发”从移动端迁移到服务器，同时保留移动端图片分析和本地相机分析能力。

**一路流可能被多个用户同时观看，推理必须在服务端统一完成一次。** App 不在接收到的远程流上做任何推理；App 的端侧 YOLO 只用于本地图片和相机预览实时帧。

### 1.3 产品边界

服务端负责：

- 视频流会话管理、接入、断线重连和生命周期控制；
- 视频帧解码、限帧、YOLO 推理、可选叠加和 H.264 编码；
- 把每路流的编码结果推给媒体服务器（MediaMTX），由其扇出给多个观看者；
- 以独立通道下发结构化检测结果（类别、置信度、坐标）；
- 暴露健康检查、流状态、模型状态和 YOLO 开关接口；
- 提供面向运维人员的轻量级 Web 管理控制台。

服务端暂不负责：

- 修改现有 Android 端代码或替代移动端图片/相机本地 YOLO；
- 账号体系、租户计费、复杂工单和业务后台；
- 训练 YOLO 模型、标注数据管理和模型自动发布；
- 直接承诺公网高并发直播平台能力；
- 为每个观看者单独编码，或自研扇出、重连和拥塞控制 —— 这些由媒体服务器和 WebRTC 承担。

### 1.4 目标用户与使用场景

| 用户/系统 | 主要动作 | 关注点 |
|---|---|---|
| 移动巡检 App | 创建会话、推送视频、切换 YOLO、用 WHEP 播放结果流、订阅检测结果 | 延迟、稳定性、接口简单 |
| 巡检人员 | 查看原始画面或带框画面 | 画面连续、目标框准确 |
| 服务运维人员 | 查看流状态、模型状态和错误 | 可观测性、重启恢复 |
| 算法工程师 | 替换模型、调整置信度和推理策略 | 模型隔离、可配置、可回滚 |

## 2. 目标架构

```text
输入：移动端推帧 / RTSP / HTTP / 文件 / 摄像头
    │
    ▼
输入适配层 → 流会话管理 → 最新帧队列/限帧（latest-only）
                         │
              YOLO 关闭 ─┴─ 原帧
              YOLO 开启 ─── 解码 → 推理 → 结构化结果 →（可选）叠加
                         │
                    H.264 编码（每路流只编一次）
                         │
                    推流到 MediaMTX
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
  WebRTC / WHEP      LL-HLS            RTSP
  App、浏览器        回退链路          诊断工具
  （生产）                            （不进 App）

检测结果 JSON ──→ 独立 WebSocket 旁路（与视频帧解耦）
MJPEG / WS JPEG ──→ 管理页预览、自动化测试、联调探针（非生产）
```

设计原则：

1. **三面分离**：控制面走 REST/JSON，视频面走 H.264（WebRTC/LL-HLS/RTSP），元数据面走独立 WebSocket；
2. **只编码一次**：每路流一次 H.264 编码，扇出由媒体服务器完成，服务端不按订阅者数量放大计算和带宽；
3. **最新帧优先**：不堆积过期帧，服务器繁忙时丢弃旧帧，优先保障实时性；
4. **分析可切换**：同一会话内可随时开关 YOLO，关闭后不触发模型推理；
5. **推理结果结构化**：推理返回结构化检测结果，画框叠加是可选的下游步骤，不能是唯一产物；
6. **模型惰性加载**：首次开启时加载模型，模型不可用时保留原始视频能力并返回明确错误；
7. **输入输出解耦**：更换输入源或增加分发协议不改变上层会话和 YOLO 控制接口。

## 3. 功能需求

### FR-01 流会话管理（必须）

- 创建唯一 `stream_id`；重复创建返回 `409`；
- 支持客户端推帧会话和服务端拉流会话；
- 查询会话状态：连接状态、YOLO 状态、接收帧数、处理帧数、最近错误；
- 删除会话时释放 WebSocket、VideoCapture、模型相关资源；
- 服务重启后不要求恢复旧会话，客户端应能自动重新创建。

### FR-02 视频接入（MVP 必须）

- WebSocket 接收二进制 JPEG/PNG 帧；非法图片返回可定位错误，不得导致进程退出；
- 支持配置 `source_url` 拉取 RTSP、HTTP、本地文件或摄像头索引；
- 拉流失败后自动释放资源并按退避策略重试；
- 支持输入帧限速，避免推送端帧率高于服务器处理能力时内存增长。

### FR-03 YOLO 分析控制（必须）

- `POST /api/streams/{stream_id}/yolo` 手动设置 `enabled=true/false`；
- 开启后仅对后续接收帧执行推理；关闭后直接输出原始画面；
- 推理必须返回**结构化检测结果**（类别 id、类别名、置信度、归一化或像素坐标框），叠加是基于该结果的可选下游步骤；不允许只产出已画框的图像；
- 返回模型是否加载成功、模型路径错误或依赖缺失等状态；
- 置信度、设备（CPU/CUDA 等）和推理尺寸通过环境变量或配置文件控制；
- 模型推理必须放入线程/进程边界，不能阻塞 FastAPI 事件循环。

### FR-04 视频输出（必须）

- 每路流只做**一次** H.264 编码，编码结果推送到媒体服务器（MediaMTX），由其扇出，服务端不为每个订阅者重复编码；
- 生产播放路径是 **WebRTC，信令用 WHEP**：一次 HTTP POST 交换 SDP 即可拉流，不引入长连接信令服务器和自定义状态机；
- **LL-HLS 作为过渡与回退**：WebRTC 打不通（UDP 被封、NAT 穿透失败、客户端不具备条件）时降级使用；早期没有 WebRTC 客户端时也用它先跑通链路；
- **RTSP 仅用于诊断**：供 VLC/ffprobe 等工具连接排障，不作为 App 的播放路径；
- **MJPEG / WebSocket JPEG 保留但降级**：只用于管理页预览、自动化测试和联调探针，不是任何客户端的生产输出；
- 同一流支持多个观看者，观看者断开不得影响输入会话，也不得影响其他观看者；
- 无新帧时不发送忙循环空包；
- 输出应携带当前处理模式对应的画面：原始或 YOLO 叠加画面；
- 控制面必须能查询到每路流的播放地址（WHEP / LL-HLS / RTSP）和当前可用性。

### FR-05 状态与诊断（必须）

- `GET /healthz` 仅表示进程存活；
- 流状态需要区分“已创建、接收中、输出中、错误、已关闭”；
- 记录最近一次输入、推理、编码和推流错误；
- 记录接收 FPS、处理 FPS、编码 FPS、输出订阅数和累计丢帧数；
- 暴露到媒体服务器的推流状态（已连接/重连中/失败）和各播放协议的可用性；
- 日志不得输出视频内容、模型权重或敏感凭据。

### FR-06 安全（生产必须，联调可简化）

- 生产环境必须启用 TLS 或置于可信内网网关之后；
- 控制接口必须鉴权，至少区分“查看”和“控制 YOLO/删除流”；
- 限制单个流的最大分辨率、帧率、并发订阅数和消息大小；
- `stream_id` 只允许安全字符，避免路径穿越和日志注入；
- 媒体服务器的 RTSP 诊断端口不得暴露到公网；WHEP/LL-HLS 端口对外时必须经网关鉴权；
- 不把模型文件、访问令牌和 `.env` 提交到仓库。

### FR-07 管理控制台（P1 必须）

- 通过 `/` 或 `/admin/` 提供 Web 控制台，不要求单独部署前端工程；
- 展示活跃流数量、YOLO 分析数量、累计接收帧和模型状态；
- 展示流会话列表，并支持选择流查看 MJPEG 实时预览；
- 支持创建、删除流会话；
- 支持对选中流开启/关闭 YOLO；
- 支持调整单路置信度阈值和最大处理 FPS，并自动调用管理 API 保存；
- 展示当前流的原始/YOLO 模式、帧数和最近状态；
- 管理页面只调用管理 API，不直接操作视频处理对象；
- 生产环境必须为管理页面和管理 API 增加鉴权，移动端业务令牌不能直接获得删除流和修改模型参数的权限。

### FR-08 检测结果元数据通道（必须）

- 结构化检测结果以 JSON 走**独立 WebSocket 旁路**，与视频帧解耦，订阅视频不强制订阅元数据，反之亦然；
- 每条消息至少包含 `stream_id`、帧序号或时间戳、检测数组（类别、置信度、坐标框）和模型标识；
- 观看者可各自决定是否需要元数据、是否在客户端自行绘制，服务端不因此改变视频路径；
- 元数据通道断开不得中断视频输出，视频断开不得中断元数据通道；
- 该通道是 App 做交互、统计和事件的唯一数据来源，不允许要求 App 从画面像素反推检测结果。

### FR-09 H.264 编码与分发约束（必须）

- 编码参数必须满足 API 27 设备硬解码能力：Profile Baseline 或 Main、Level ≤ 4.0、**不使用 B 帧（`bf=0`）**；
- GOP 1–2 秒（25fps 下 25–50 帧），`tune=zerolatency`，CBR 或 CRF + `maxrate`/`bufsize`；
- 目标档位 720p @ 15–25fps、2–3 Mbps；
- 可用时使用硬件编码器（`h264_nvenc` / `h264_qsv`），不可用时回落 libx264；
- 编码器实现在 PyAV 进程内编码与 FFmpeg 子进程管道之间按实测选择，两者都必须满足上述约束；
- 媒体服务器为 MediaMTX 1.20.1（MIT，Copyright (c) 2019 aler9），部署拓扑必须包含其 UDP 端口 `:8189` 与 HTTP 端口 `:8888`（LL-HLS）、`:8889`（WebRTC/WHEP），RTSP 端口 `:8554` 仅对诊断网络开放；
- 媒体服务器进程的启动、健康检查和异常重启必须纳入部署与运维流程，不能假设它总是在线。

## 4. 非功能需求

### NFR-01 实时性

目标：局域网环境下，从服务器收到一帧到 WHEP 播放端显示的端到端 P95 延迟不超过 500 ms；关闭 YOLO 时 P95 不超过 200 ms。延迟统计必须包含解码、推理、叠加、**H.264 编码**、推流、媒体服务器转发和排队时间。

该目标当前**未经实测**（见第 9 章未验证项）。LL-HLS 回退路径的延迟目标另行给定，不套用 WebRTC 的 500 ms。

### NFR-02 稳定性

- 连续运行 2 小时不因单帧损坏、客户端断开或模型异常而进程退出；
- 输入断开后 10 秒内可识别并释放/重试；
- 观看者退出不造成会话泄漏；
- 媒体服务器不可用或重启时，服务端必须能识别推流失败、重建推流，且不因此丢掉输入会话；
- 服务能够优雅关闭并释放所有活动会话。

### NFR-03 资源控制

- 默认采用 latest-only 策略，队列深度不超过 1 个待处理帧；
- YOLO 关闭时不得加载模型或调用推理；
- CPU、内存、GPU 使用率需可通过日志或指标观察；
- 单路 1080p 输入的资源上限应在真实部署机上压测确认，不能仅按模型参数估算；
- **加入 H.264 编码后，M02-T03 的旧容量与资源数字不再代表实际负载，必须重新压测**；编码占用的 CPU/GPU 要与推理分开计量。

### NFR-04 可部署性

- 支持 Python 虚拟环境直接启动；
- 支持 Docker Compose 部署；
- 媒体服务器（MediaMTX）作为部署拓扑的一部分一起编排，服务端启动时不假设它已就绪；
- 模型通过只读挂载注入，代码镜像与模型权重分离；
- 配置通过环境变量覆盖默认值。

## 5. 接口契约

服务端与移动端的联调协议见 [docs/video-protocol.md](docs/video-protocol.md)，机器可读请求 Schema 见 [docs/video-protocol.schema.json](docs/video-protocol.schema.json)，输出传输协议决策见 [docs/adr-002-production-transport.md](docs/adr-002-production-transport.md)。

控制面（REST/JSON，服务端 `:8080`）：

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/healthz` | 服务存活检查 |
| POST | `/api/streams` | 创建视频流会话 |
| GET | `/api/streams` | 查询所有会话 |
| DELETE | `/api/streams/{stream_id}` | 删除会话 |
| POST | `/api/streams/{stream_id}/yolo` | 开关 YOLO |
| PATCH | `/api/streams/{stream_id}/config` | 调整置信度、最大 FPS |
| GET | `/api/streams/{stream_id}/playback` | 查询该流的 WHEP / LL-HLS / RTSP 播放地址与可用性 |
| GET | `/api/metrics` | 进程与各流的 FPS/延迟/丢帧指标 |

数据面：

| 面 | 通道 | 用途 |
|---|---|---|
| 输入 | WS `/api/streams/{stream_id}/ingest` | 移动端推送 JPEG/PNG 帧 |
| 视频（生产） | WHEP `http://<media>:8889/{path}/whep` | App、浏览器播放，H.264 |
| 视频（回退） | LL-HLS `http://<media>:8888/{path}/index.m3u8` | WebRTC 打不通时降级 |
| 视频（诊断） | RTSP `rtsp://<media>:8554/{path}` | VLC/ffprobe 排障，不进 App |
| 元数据 | WS `/api/streams/{stream_id}/detections` | 结构化检测结果 JSON |
| 测试与诊断 | `GET /api/streams/{stream_id}/mjpeg`、WS `/api/streams/{stream_id}/ws` | 管理页预览、自动化测试、联调探针（非生产） |

创建会话请求：

```json
{"stream_id":"inspection-001","source_url":null}
```

YOLO 控制请求：

```json
{"enabled":true}
```

YOLO 控制响应至少包含：

```json
{"stream_id":"inspection-001","yolo_enabled":true,"model_error":null}
```

检测结果消息至少包含：

```json
{"stream_id":"inspection-001","frame_seq":1024,"captured_at":"2026-09-03T10:00:00.123+08:00",
 "model":"yolo11n","detections":[{"class_id":0,"class_name":"person","confidence":0.87,
 "box":{"x":0.31,"y":0.22,"w":0.12,"h":0.34}}]}
```

## 6. 业务流程与状态

```text
CREATED → INGESTING → OUTPUTTING
             │             │
             ├→ ERROR ←────┘
             └→ CLOSED
```

- `CREATED`：会话已创建，尚未收到有效帧；
- `INGESTING`：正在接收或拉取视频；
- `OUTPUTTING`：至少有一帧可供观看者读取；
- `ERROR`：最近一次操作失败，但会话仍可恢复；
- `CLOSED`：会话已删除，不再接受新帧。

YOLO 是独立于会话状态的模式开关：`OFF`、`ON`。模型不可用时，`ON` 请求应返回错误信息，但不能破坏 `INGESTING` 或原始画面输出。

## 7. 版本路线图

### 已交付基线（原 P0/P1，对应 M00–M03、M06）

- JPEG WebSocket 输入；
- RTSP/HTTP/文件/摄像头拉流输入；
- YOLO 手动开关、置信度与最大 FPS 单路配置；
- MJPEG/WebSocket JPEG 输出（现降级为测试与诊断通道）；
- 轻量级 Web 管理控制台；
- latest-only 丢帧统计、FPS、延迟和订阅者指标；
- 模型注册、哈希校验、PT/ONNX/TFLite 转换与服务端模型切换；
- 健康检查、流状态和单元测试。

这些基线的容量与延迟数字取自加入 H.264 编码之前的实现，不能直接沿用。

### 当前阶段：生产传输协议（ADR-002，对应 M08 服务端 H.264 编码与 WebRTC 分发实现）

- 推理返回结构化检测结果，`detector.annotate()` 重构为 `detect()`，叠加下移为可选步骤（`M08-T01`）；
- 每路流一次 H.264 编码，编码参数按 FR-09 固定（`M08-T02`）；
- 推流到 MediaMTX，由其提供 WebRTC/WHEP、LL-HLS 和 RTSP（`M08-T03`、`M08-T06`）；
- 检测结果 WebSocket 旁路（`M08-T05`）；
- 控制面暴露播放地址查询（`M08-T04`）；
- 管理控制台适配新输出，并把 MJPEG/WebSocket JPEG 标记为诊断通道（`M08-T07`）；
- 按流绑定模型，替换单个全局 `activeServerModel`（`M08-T09`）；
- 实测 WHEP 首帧与端到端 P50/P95 延迟，验证或推翻 500 ms 目标（`M08-T08`）；
- 加入编码后重新压测单机并发容量与网络韧性（改写后的 `M04-T03`、`M04-T04`）。

### 生产候选版本

- 场景化模型市场元数据（烟雾识别、异物入侵、玩手机识别等分类），并可在 App 侧展示；
- 鉴权、TLS、审计日志、Prometheus 指标和告警；
- 公网部署的 STUN/TURN 拓扑与带宽核算；
- 事件规则与告警回传（远程流的事件在服务端产生，落点待定）；
- 录像、回放与检测结果持久化。

### 模型资产与转换需求

服务器项目需要同时管理两类模型产物：

- 服务端模型：PT 或 ONNX，用于服务器 YOLO 视频推理；
- 移动端模型：LiteRT/TFLite，用于保留的图片分析和本地相机分析。

必须支持从同一份 PT 权重生成两类产物，并为每次转换生成 Manifest，记录输入尺寸、输出张量、类别标签、量化方式、文件哈希、工具链版本和许可证状态。管理 API/页面应能登记转换 Manifest、重新校验产物哈希并列出模型。模型管理页面只能激活已存在且哈希匹配的服务端模型；移动端 TFLite 资产可查看但不能当作服务端活动模型。

## 8. 验收标准

1. 不修改 `mobile-app/` 的任何文件；
2. 空模型环境下服务可启动、可接收帧、可输出原始视频；
3. YOLO 关闭时不会调用模型推理；
4. YOLO 开启且模型存在时，推理返回结构化检测结果，且叠加模式下输出帧包含画框；
5. 非法帧、断开连接和拉流失败不会导致服务进程退出；
6. 同一流至少支持一个输入端和两个 WHEP 观看者，且服务端只编码一次；
7. 检测结果 WebSocket 与视频通道可独立断开重连，互不影响；
8. WebRTC 不可用时能按 LL-HLS 回退，且回退是显式可观测的，不是静默失败；
9. 单元测试覆盖会话、YOLO 开关、结构化检测结果和 API 基础链路；
10. 部署人员按照 README 可在虚拟环境或 Docker 中启动服务与媒体服务器；
11. 真实部署前必须补充目标服务器和实际模型上的延迟、吞吐、温度及连续运行测试。

## 9. 未验证项与后续决策点

### 9.1 未验证项（ADR-002 中的假设，不得当作已验证）

1. **libwebrtc 在 API 27 上的可用性与体积**：预估 arm64 约 10 MB APK 增量，未实测；API 27 硬解码行为未验证。若证伪，按 ADR-002 回退到 LL-HLS 作为 App 主链路。
2. **公网 NAT 穿透**：局域网可直连；公网需要 STUN，可能需要 TURN 中继（TURN 会把带宽成本转回服务端）。拓扑与端口策略未定。
3. **加入编码后的单机并发容量**：M02-T03 的旧容量数据不再成立，需重新压测。
4. **实测端到端延迟**：WHEP 链路的 P50/P95 尚无测量值，NFR-01 的 500 ms 目标未经验证。

### 9.2 仍需真实数据才能写死的事项

- 编码器实现选 PyAV 进程内编码还是 FFmpeg 子进程管道；
- 是否需要同时提供原始流和分析流两个独立播放地址；
- YOLO 是否增加 ByteTrack，以及事件规则引擎放在服务端还是 App 端（远程流的事件在服务端产生，服务端当前没有事件模块）；
- 单机需要支持的并发流数量；
- 服务器使用 CPU、CUDA GPU 还是其他推理加速卡；
- 视频是否需要录像、回放和检测结果持久化；
- 已冻结的 5 类业务类别（`bottle/bag/box/tool/person`）与新的多场景模型市场如何合并。

以下事项已由 ADR-002 决定，不再是开放决策点：移动端播放协议（WebRTC/WHEP，回退 LL-HLS）、是否保留 MJPEG 作为生产协议（不保留）、扇出是否自研（不自研，用 MediaMTX）。
