# Harness任务板

> 本文件由`python harness/harness.py render`生成，请勿手工修改。  
> 更新时间：2026-09-12T16:48:34+08:00

状态：`[ ] pending`、`[~] in_progress`、`[!] blocked`、`[x] completed`、`[-] superseded`。

> `superseded`表示任务定义因架构或范围变更而作废：不再执行，也不计入完成。
> 终止原因见`harness/records/<大任务>/<任务>.superseded-*.md`。

## [x] M00 服务基础能力与管理后台 MVP

大任务状态：`completed`  
依赖：无  
验收文档：`harness/acceptance/M00-foundation.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M00-T01 | 建立 Python 服务骨架与部署配置 | 无 | FastAPI 服务、依赖文件、环境变量示例和 Docker 配置齐全 |
| [x] | M00-T02 | 实现视频流会话与输入输出链路 | M00-T01 | 支持 JPEG WebSocket 推帧、RTSP/文件拉流、MJPEG/WebSocket 输出和资源释放 |
| [x] | M00-T03 | 实现 YOLO 手动开关与失败降级 | M00-T02 | YOLO 开关、置信度配置、惰性加载和模型缺失时原始流降级均可验证 |
| [x] | M00-T04 | 实现轻量级 Web 管理后台 | M00-T02, M00-T03 | 管理页面可查看流、实时预览、开关 YOLO、调整参数、创建和删除会话 |
| [x] | M00-T05 | 建立服务器项目 Harness 与任务看板 | 无 | Harness CLI、任务状态、任务板、测试和验收文档均可校验 |

## [x] M01 移动端联调协议与真实视频链路

大任务状态：`completed`  
依赖：M00  
验收文档：`harness/acceptance/M01-mobile-integration.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M01-T01 | 冻结服务端与移动端视频协议 | 无 | 明确编码格式、帧大小、帧率、会话状态、控制接口、错误码和鉴权占位 |
| [x] | M01-T02 | 制作移动端推帧联调客户端 | M01-T01 | 在不修改正式 mobile-app 代码的前提下，提供可重复推送测试帧或测试视频的客户端工具 |
| [x] | M01-T03 | 验证服务端输出流播放器兼容性 | M01-T01 | 自动化验证浏览器管理页、MJPEG 响应和 WebSocket JPEG 输出契约，并提供可重复的 VLC/Android 现场播放验证手册；现场设备结果不得虚构 |
| [x] | M01-T04 | 完成真实端到端视频链路验收 | M01-T02, M01-T03 | 本地真实视频从输入、服务端处理到输出闭环通过，并记录进程内延迟；Android 现场验证缺口单独冻结记录 |

## [x] M02 服务器 YOLO 生产推理管线

大任务状态：`completed`  
依赖：M01  
验收文档：`harness/acceptance/M02-yolo-pipeline.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M02-T01 | 接入并验证实际 YOLO 模型 | 无 | 目标模型、类别顺序、输入尺寸、置信度和推理设备记录完整，真实帧叠加结果正确 |
| [x] | M02-T02 | 实现 latest-only 推理调度与背压 | M02-T01 | 推理不积压旧帧，服务器过载时可丢帧并提供丢帧统计 |
| [x] | M02-T03 | 增加推理性能和运行状态指标 | M02-T02 | 可记录接收 FPS、推理 FPS、输出 FPS、端到端延迟、P95 和资源使用率 |
| [x] | M02-T04 | 验证模型失败与运行时降级 | M02-T01, M02-T02 | 模型损坏、依赖缺失、推理异常和显存不足均有明确状态且原始流可按策略保留 |

## [x] M03 管理后台安全与可观测性

大任务状态：`completed`  
依赖：M00  
验收文档：`harness/acceptance/M03-admin-observability.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M03-T01 | 实现管理后台鉴权与权限边界 | 无 | 查看、控制、删除和模型配置权限分离，未授权请求被拒绝 |
| [x] | M03-T02 | 增加服务指标与诊断日志 | M02-T03 | 管理页面或指标端点可查看流状态、延迟、FPS、丢帧、CPU/内存/GPU 和错误 |
| [x] | M03-T03 | 完成 TLS、限流和消息大小保护 | M03-T01 | 控制接口、WebSocket 输入和输出具备超时、大小、并发和速率限制 |

## [~] M04 生产传输协议与多流扩展

大任务状态：`in_progress`  
依赖：M01, M02  
验收文档：`harness/acceptance/M04-production-transport.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M04-T01 | 对比 MJPEG、WebRTC、SRT 输出方案 | M01-T04 | 同一输入、同一服务器和播放器下形成延迟、带宽、兼容性和许可证对比报告 |
| [-] | M04-T02 | 实现生产低延迟输出适配器 | M04-T01 | 选定协议可被目标 Android 播放器稳定接收，端到端延迟达到验收目标 |
| [x] | M04-T03 | 完成多流并发与容量压测 | M02-T03, M08-T03, M08-T06 | 在包含 H.264 编码与推流的完整链路上明确单机并发流数量、资源上限、降级策略和扩容指标；编码开销单独计量，M02-T03 的无编码容量数字不得沿用 |
| [x] | M04-T04 | 完成弱网、重启和故障恢复测试 | M08-T03, M08-T06 | 断流、丢包、播放器断开、WHEP 回退 LL-HLS、服务重启和媒体服务器重启后均可按文档恢复，且无会话与推流资源泄漏 |

## [ ] M05 发布、运维与长期稳定性

大任务状态：`pending`  
依赖：M03, M04  
验收文档：`harness/acceptance/M05-release.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [ ] | M05-T01 | 完善生产 Docker 与配置管理 | 无 | 支持环境隔离、只读模型挂载、健康检查、日志输出和配置校验，并覆盖服务端与媒体服务器的编排与端口暴露策略 |
| [ ] | M05-T02 | 完成连续运行与故障注入测试 | M04-T04 | 至少完成 2 小时连续运行和输入/模型/网络故障注入，服务无不可恢复崩溃 |
| [ ] | M05-T03 | 生成依赖、模型和接口发布清单 | M03-T03, M05-T01 | SBOM、许可证、模型哈希、配置项、API 文档和部署版本均可追溯，含媒体服务器与 FFmpeg 组件的版本、哈希与许可证 |
| [ ] | M05-T04 | 完成生产候选版本验收 | M05-T02, M05-T03 | 发布候选版本完成安装、升级、回滚、健康检查和关键业务流程验收 |

## [x] M06 模型资产迁移与转换管理

大任务状态：`completed`  
依赖：M02  
验收文档：`harness/acceptance/M06-model-management.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M06-T01 | 迁移并登记移动端现有 YOLO11n 模型资产 | 无 | PT、TFLite 三档模型、标签和 Manifest 复制到服务端并记录来源、哈希、输入输出和许可证状态 |
| [x] | M06-T02 | 实现 PT 到移动端与服务端模型的转换工具 | M06-T01 | 命令行支持 PT→TFLite 移动端模型、PT→服务端 ONNX/保留 PT，并生成可追溯 Manifest |
| [x] | M06-T03 | 接入模型注册、校验与活动模型切换 | M06-T01, M06-T02 | 管理 API 可列出模型、校验文件和切换流使用的活动模型；切换失败不影响旧模型和原始流 |
| [x] | M06-T04 | 完成模型转换与管理回归验证 | M06-T03 | 完成模型元数据、哈希、转换命令、API、管理页面和失败降级测试，真实业务精度和商业发布继续按条件冻结 |

## [x] M07 生产传输协议重构与架构重定位

大任务状态：`completed`  
依赖：无  
验收文档：`harness/acceptance/M07-transport-repositioning.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M07-T01 | 为 Harness 增加 superseded 终止状态与 supersede 命令 | 无 | CLI 支持 supersede 子命令并生成终止记录；superseded 不满足依赖、不可被 start 或 block、已完成任务不可被终止；大任务可在含 superseded 子任务时验收但不得全部 superseded；单元测试与 validate、render 全部通过 |
| [x] | M07-T02 | 冻结 ADR-002 输出传输协议决策并作废 ADR-001 | M07-T01 | ADR-002 记录 WebRTC/WHEP、LL-HLS、RTSP 和 MJPEG 的定位、H.264 编码约束、被拒方案理由和未验证项；docs/adr-001-output-protocol.md 标记为被取代 |
| [x] | M07-T03 | 按新传输协议更新产品需求与接口契约 | M07-T02 | PRODUCT_REQUIREMENTS.md、README 和协议文档反映 H.264 单次编码多路分发、REST 控制面、检测元数据 WebSocket 旁路和 MJPEG 降级为诊断通道 |
| [x] | M07-T04 | 按新协议重构服务端任务板 | M07-T02, M07-T03 | M04 及其他受影响任务完成终止或改写，新增编码、分发、播放器兼容和容量压测任务，validate 与 render 通过 |

## [~] M08 服务端 H.264 编码与 WebRTC 分发实现

大任务状态：`in_progress`  
依赖：M07  
验收文档：`harness/acceptance/M08-h264-webrtc-distribution.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M08-T01 | 重构推理输出为结构化检测结果 | 无 | 推理返回类别、置信度和归一化坐标的结构化结果，叠加基于该结果实现且可关闭，不再只产出已画框的图像 |
| [x] | M08-T02 | 实现 H.264 编码环节并选定编码器实现 | M08-T01 | PyAV 与 FFmpeg 子进程两种实现按实测选定其一并记录依据，编码参数符合 Baseline/Main、Level ≤ 4.0、bf=0、GOP 1-2 秒、tune=zerolatency，编码 FPS 与耗时可观测 |
| [x] | M08-T03 | 实现到 MediaMTX 的推流、重连与推流状态上报 | M08-T02 | 每路流以固定路径推流到媒体服务器，断连自动重连，`publish_state` 与观看者数在状态接口可见 |
| [x] | M08-T04 | 实现播放地址接口与协议可用性上报 | M08-T03 | `GET /api/streams/{stream_id}/playback` 按协议 §5.5 返回 WHEP、LL-HLS 和 RTSP 地址及可用性，RTSP 明确标记仅供诊断 |
| [x] | M08-T05 | 实现检测结果 WebSocket 旁路 | M08-T01 | `WS /api/streams/{stream_id}/detections` 按协议 §6.2 下发结构化检测结果，与视频通道相互独立，YOLO 关闭时下发心跳 |
| [x] | M08-T06 | 完成 MediaMTX 部署编排与端口暴露策略 | M08-T03 | 媒体服务器可随服务重复启动，端口暴露按 docs/production-security.md 分离控制面与媒体面，进程异常可恢复，配置与证书不入库 |
| [x] | M08-T07 | 适配管理后台的新输出与诊断通道降级 | M08-T04, M08-T05 | 管理页面展示播放地址、推流状态和检测结果，MJPEG 与 WebSocket JPEG 预览明确标记为诊断通道 |
| [x] | M08-T08 | 完成 WHEP 与 LL-HLS 播放器兼容性实测 | M08-T04, M08-T06 | 浏览器 WHEP、LL-HLS 回退和 RTSP 诊断均有实测记录并含首帧与 P50/P95 延迟；Android 侧记录设备型号、API、WebRTC 库版本，设备不可用时保持冻结且不得伪造结果 |
| [x] | M08-T09 | 实现按流绑定模型替换单一全局活动模型 | M08-T01 | 每路流可独立选择已注册模型，切换不影响其他流，注册表保留场景与用途元数据 |

## [~] M09 场景化模型市场（MVP）

大任务状态：`in_progress`  
依赖：M06, M07  
验收文档：`harness/acceptance/M09-model-marketplace-mvp.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M09-T01 | 定义模型市场目录与下载 API 契约 | 无 | 契约文档给出目录列表、模型详情和产物下载三类接口的请求响应字段、错误码与鉴权要求，明确 MVP 不含签名，并与 App 端 M14-T02/M14-T03 的实现契约一致 |
| [x] | M09-T02 | 建立场景模型目录数据源 | M09-T01 | 场景模型登记在单一数据源中，含 modelId、版本、场景、运行时、类别标签、产物路径、大小和 SHA-256；服务启动时校验产物存在且校验值匹配，缺失或不符时明确报错而不静默降级 |
| [x] | M09-T03 | 实现模型市场目录查询接口 | M09-T02 | 提供市场目录列表与模型详情接口，支持按场景过滤，返回字段符合 M09-T01 契约；未授权请求被拒绝，响应与日志不含本机绝对路径 |
| [x] | M09-T04 | 实现模型产物下载与完整性校验 | M09-T02 | 提供产物下载接口并给出大小与 SHA-256，客户端下载后可校验一致；超出并发或速率限制返回约定错误码，日志不输出令牌 |
| [x] | M09-T05 | 上架首个业务场景模型（异物入侵五类） | M09-T03, M09-T04, M09-T08 | 异物入侵五类模型的服务端与 Android 产物成对上架，Manifest 给出类别标签与输入尺寸，转换链路复用 M06 成果并记录模型来源、版本与许可证结论 |
| [x] | M09-T06 | 实现管理页模型市场视图与按流绑定展示 | M09-T03, M08-T09 | 管理页可查看市场模型列表与每路流当前绑定的模型，绑定变更后取流侧生效；页面不展示令牌与本机绝对路径 |
| [x] | M09-T07 | 完成模型市场自动化测试 | M09-T04, M09-T05 | 覆盖目录查询、鉴权失败、下载校验值一致、校验值不符和限流的自动化测试全部通过，且测试不依赖真实大体积模型文件 |
| [x] | M09-T08 | 冻结模型来源与许可证决策（ADR-003） | 无 | ADR-003 记录：采用路线 A（保留 Ultralytics YOLO 系列并购买 Enterprise License，模型层保持可替换）；官方权重仅 COCO 与 ImageNet；按可获得性把智慧工地场景包收敛为工地合规、烟火、抽烟、物料现场四组，逐条列出被移除的类别与移除原因；标签契约使用权重原始类名，中文显示名在 Manifest 映射层；Manifest 增加 license、来源 URL、上游版本字段；同时记录 Apache-2.0 备选路线的触发条件 |

## [~] M10 模型签名与发布服务

大任务状态：`in_progress`  
依赖：无  
验收文档：`harness/acceptance/M10-model-signing-publish.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M10-T01 | 拆解模型签名与发布服务任务 | M09-T05 | 在模型市场 MVP 可用后，输出签名算法与密钥保管方案、发布与灰度流程、App 端对接契约，并把本大任务拆成可执行小任务；拆解完成前不实现任何签名逻辑 |
| [x] | M10-T02 | 实现 Ed25519 Manifest 签名与下载契约 | M10-T01 | 服务端按 App schemaVersion=1 的固定规范载荷生成 Ed25519 签名 Manifest；私钥仅从受控配置加载，目录 API 暴露 Android Manifest 与 TFLite 产物；缺失、无效或不受信任配置不得伪造可安装状态；单元和接口测试通过 |
| [ ] | M10-T03 | 实现签名密钥轮换与发布门禁 | M10-T02 | 签名配置支持稳定 keyId 与新旧公钥并存窗口；撤销密钥和未签名模型不能发布为 releaseEligible；后台诊断不泄露私钥，发布、下线与失败恢复测试通过 |

## [~] M11 公共告警引擎（规格冻结与服务端实现）

大任务状态：`in_progress`  
依赖：M07  
验收文档：`harness/acceptance/M11-alert-engine.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M11-T01 | 冻结公共告警引擎规格与 JSON Schema | 无 | 产出 docs/alert-engine-spec.md 与 docs/alert-engine.schema.json，唯一定义五层结构、subjectKey 五类（track/roi/metric/source/frame）、算子集与各自所需能力、三档严重级别、矩形与多边形与线段 ROI 几何、七类阈值及其运行时可改要求；明确求值器为纯函数且时间只来自观测；Schema 预留 RELATION、METRIC_THRESHOLD、COMPOSITE 与短视频存证并标注未实现；App 侧 M15 全部小任务以本契约为准 |
| [x] | M11-T02 | 建立双侧共用的一致性金样向量 | M11-T01 | 产出 docs/alert-engine-conformance/ 下的“观测序列 → 期望事件”JSON 向量，覆盖每个已实现算子、三档分档、未处置提级、同级冷却抑制与级别提升放行、能力不满足显式拒绝；其中一组向量从 App M07 既有行为反推，用于锁定不得改变的语义；向量由服务端测试套件加载执行，且可被 App 原样复制运行 |
| [x] | M11-T03 | 实现观测接入与观测信封 | M11-T01, M08-T01 | 引擎入口接受统一观测信封（检测框、分类结果、分割掩膜、标量读数）与来源标识、帧序号和时间戳；不带框的分类型观测不得进入需要框的算子，而是被显式拒绝；latest-only 丢帧不破坏时间单调性断言 |
| [x] | M11-T04 | 实现多边形与线段 ROI 几何 | M11-T01 | ROI 支持矩形、多边形与线段三种形态：多边形提供点在多边形内与框和多边形相交判定，线段提供跨越方向判定；退化输入（少于三点、自相交、零长线段）在构造时被拒绝；几何为纯函数并有金样向量覆盖 |
| [x] | M11-T05 | 实现算子求值集 | M11-T02, M11-T03, M11-T04 | 实现 PRESENCE、IN_REGION、LINE_CROSS、DWELL、COUNT、AREA_RATIO、RATE、ABSENCE 八个算子；RATE 的滑动窗口内存有界且过期数据被回收；每个算子在金样向量上的输出与期望事件一致 |
| [x] | M11-T06 | 实现事件生命周期与三档严重级别 | M11-T05 | 事件状态机支持候选、确认、结束与冷却；严重级别按阈值分档在同一时刻的事实上判定，未处置超时提级只提升通知级别不改写原始事实；冷却只抑制同级重复，级别提升与未处置提级必然放行；事件快照记录生效阈值、规则版本与 subjectKey |
| [x] | M11-T07 | 实现规则管理、能力声明与不兼容拒绝 | M11-T05 | 规则声明所需能力（框、掩膜、跟踪、标量、多边形），绑定到不满足能力的来源或模型时返回显式拒绝并可在管理页看到原因，不允许静默不触发；规则、阈值与 ROI 可运行时新增修改删除且不重启进程，变更有审计记录 |
| [x] | M11-T08 | 实现告警投递通道 | M11-T06 | 投递覆盖管理页实时推送、App 内通知下发与邮件/短信/企业 IM 三类外部通道适配器；外部通道以接口加本地假实现交付并留出凭据注入点，凭据不进日志与仓库；不实现面向上游系统的 Webhook 回调；投递失败有重试上限且不阻塞事件生成 |
| [x] | M11-T09 | 实现处置闭环与误报回流 | M11-T06 | 事件支持确认与误报标记，两种动作记录操作人与时间戳；误报样本连同截图、生效阈值与检测结果导出为可再训练的数据集条目，导出内容不含令牌与本机绝对路径；确认与误报标记的权限与只读查看权限分离 |
| [ ] | M11-T10 | 完成告警引擎自动化测试与金样一致性验证 | M11-T05, M11-T06, M11-T07, M11-T08, M11-T09 | 服务端自动化测试覆盖八个算子、三档分档、冷却与提级、能力拒绝、运行时改配与处置闭环；M11-T02 的全部金样向量在服务端通过；测试不依赖真实模型权重与外部通道凭据 |
| [x] | M11-T11 | 实现模型参数档案与后台管理页 | M11-T07 | 按模型ID、模型版本与平台持久化可调整的检测参数和标签告警初始参数；提供查询、保存、重置API，执行范围、标签一致性和跨字段校验并保留修订与审计；管理后台模型资产页使用真实API完成平台切换、编辑、保存和重置；自动化测试覆盖存储、权限、接口和页面契约；本任务不修改Android同步且不宣称告警投递链路已接通 |
| [x] | M11-T12 | 为模型参数术语增加可访问解释提示 | M11-T11 | 模型参数弹窗中的识别置信度、IoU去重阈值和单帧最大结果数显示统一帮助图标；鼠标悬停、键盘聚焦和触屏点击均可读取中文解释；提示不遮挡输入控件并适配窄屏；页面契约测试和浏览器交互验证通过 |
| [x] | M11-T13 | 为类别告警参数表头增加术语解释 | M11-T12 | 类别告警初始参数表头的启用、类别、业务名称、事件编码、置信度、连续帧、停留时间、冷却时间和级别显示统一帮助图标；鼠标悬停、键盘聚焦和触屏点击均可读取中文解释，支持Esc与空白关闭且不改变现有编辑交互；页面契约测试、后台回归测试、Harness单元测试、validate和render通过 |
| [x] | M11-T14 | 完善模型参数范围与默认值提示 | M11-T13 | 模型参数页对识别置信度、IoU去重阈值、单帧最大结果数及类别告警阈值明确展示最小值、最大值、步进和默认值；输入控件具备浏览器端范围约束，提示包含中文解释和取值范围；服务端校验与页面契约、后台回归、Harness门禁通过 |

## [x] M12 运维管理后台界面原型

大任务状态：`completed`  
依赖：无  
验收文档：`harness/acceptance/M12-admin-prototype.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M12-T01 | 搭建管理后台产品原型与 Mock 数据视图 | 无 | 重构 /admin 为带侧边导航的工业巡检管理台，覆盖总览、视频流、告警中心、规则配置、模型资产与系统诊断；内置 Mock 数据和原型交互可独立运行；不要求接通真实 API，但保留后续接入边界并通过静态页面与服务端回归测试 |
| [x] | M12-T02 | 优化管理后台为成熟 B 端视觉规范 | M12-T01 | 在不改变页面信息架构和 Mock 交互的前提下，降低装饰性渐变、霓虹色和过大标题等 AI 生成感；统一灰阶、间距、表格、状态标签、卡片和响应式规范，使侧栏固定、内容独立滚动且桌面端更接近成熟企业运维后台 |
| [x] | M12-T03 | 统一告警中心筛选按钮样式 | M12-T02 | 告警中心全部、严重、重要、一般筛选按钮不再继承旧深色主题，默认、悬停与选中状态符合新版浅色 B 端规范，告警列表行按钮边框和背景保持一致 |
| [x] | M12-T04 | 调整总览需要关注列表文字层级 | M12-T03 | 总览页需要关注列表中的事件标题、来源时间和严重级别在白色卡片上具有清晰对比度，文字层级与新版 B 端配色一致 |
| [x] | M12-T05 | 补全管理后台静态演示流程 | M12-T04 | 流详情、告警详情处置、规则编辑、模型市场安装状态和协议诊断均具备可点击 Mock 交互，不改变真实 API |

## [x] M13 管理后台事件证据图片接入

大任务状态：`completed`  
依赖：M12  
验收文档：`harness/acceptance/M13-event-evidence-images.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M13-T01 | 映射证据图片并接入告警详情弹窗 | 无 | 将证据图片复制到管理后台静态资源目录，Mock 告警绑定 evidence 字段，告警详情弹窗展示图片和降级占位 |

## [x] M14 管理后台数据看板

大任务状态：`completed`  
依赖：M13  
验收文档：`harness/acceptance/M14-admin-dashboard.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M14-T01 | 新增管理后台数据看板视图 | 无 | 在 /admin 中增加数据看板导航、指标摘要、趋势图、类别排行和流状态视图，使用现有 Mock 数据并支持基础筛选交互 |

## [x] M15 管理后台数据看板大屏模式

大任务状态：`completed`  
依赖：M14  
验收文档：`harness/acceptance/M15-dashboard-focus-mode.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M15-T01 | 增加数据看板全屏展示模式 | 无 | 看板按钮可切换 CSS 大屏状态并请求浏览器原生全屏；大屏状态只保留看板内容，退出时恢复系统导航与头部；原生全屏退出可同步页面状态 |

## [x] M16 数据看板大屏铺满与四路视频墙

大任务状态：`completed`  
依赖：M15  
验收文档：`harness/acceptance/M16-dashboard-stream-wall.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M16-T01 | 铺满大屏并新增四路视频流墙 | 无 | 大屏看板填满可用画布，新增四路带状态信息的 Mock 视频流卡片，桌面四列且移动端响应式排列 |

## [x] M17 数据看板田字型中心视频布局

大任务状态：`completed`  
依赖：M16  
验收文档：`harness/acceptance/M17-dashboard-center-video-layout.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M17-T01 | 重排大屏为中心视频田字布局 | 无 | 大屏桌面端使用固定视口网格，四路 Mock 视频在中心 2×2 展示，周围放置指标、趋势、健康、类别和处置面板，页面不产生滚动条 |

## [x] M18 数据看板信息密度优化

大任务状态：`completed`  
依赖：M17  
验收文档：`harness/acceptance/M18-dashboard-density.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M18-T01 | 补充大屏实时动态与运行摘要 | 无 | 在看板外围增加实时告警动态、流健康明细和运行摘要，桌面大屏无滚动且保持中心视频突出 |

## [x] M19 数据看板服务器状态统计

大任务状态：`completed`  
依赖：M18  
验收文档：`harness/acceptance/M19-dashboard-server-status.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M19-T01 | 增加服务器资源状态统计 | 无 | 右侧流健康面板下方展示 CPU、内存、GPU/NPU、磁盘四项资源及进度条，适配单屏大屏布局 |

## [x] M20 视频流管理静态画面统一

大任务状态：`completed`  
依赖：M19  
验收文档：`harness/acceptance/M20-stream-preview-images.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M20-T01 | 将流管理预览替换为图片画面 | 无 | 选择流会话后，LIVE INSPECTOR 使用对应本地现场图片展示预览，加载失败时显示降级提示 |

## [x] M21 管理后台 Mock 数据扩展

大任务状态：`completed`  
依赖：M20  
验收文档：`harness/acceptance/M21-admin-mock-data-expansion.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M21-T01 | 扩展管理后台告警 Mock 数据 | 无 | 在 app/static/app.js 的 MOCK.alerts 中新增 10 条记录，字段结构与现有数据一致 |

## [x] M22 管理后台模型 Mock 数据扩展

大任务状态：`completed`  
依赖：M21  
验收文档：`harness/acceptance/M22-admin-model-mock-data.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M22-T01 | 扩展管理后台模型 Mock 数据 | 无 | 在 app/static/app.js 的 MOCK.models 中新增 4 条记录，保持字段结构一致 |

## [x] M23 报警推送管理与告警通知原型

大任务状态：`completed`  
依赖：M22  
验收文档：`harness/acceptance/M23-alert-push.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M23-T01 | 搭建报警推送管理页与 Mock 发送流程 | 无 | 新增报警推送页面、Mock 配置数据、启停/测试/新增交互和告警触发模拟 |

## [x] M24 管理后台弹窗关闭行为修复

大任务状态：`completed`  
依赖：M23  
验收文档：`harness/acceptance/M24-dialog-close-fix.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M24-T01 | 统一管理后台 dialog 取消与关闭处理 | 无 | 关闭按钮显式关闭 dialog，表单只处理 submitter 为确认按钮的提交 |

## [~] M25 服务端多模态复核需求冻结与管理后台原型

大任务状态：`in_progress`  
依赖：M12, M23  
验收文档：`harness/acceptance/M25-verification-console-prototype.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M25-T01 | 冻结服务端多模态复核需求与职责边界 | 无 | 在 PRODUCT_REQUIREMENTS.md 新增多模态复核功能需求与对应验收标准，并新增 docs/multimodal-verification.md：写明复核目的、触发条件与单事件去重、每日调用上限、超时与失败一律 fail-open 保留原告警、结论只标注不删除、图像出站默认关闭且需显式授权、密钥保管要求，以及端侧复核与服务端复核的分工和真实接入的前置条件；文档明确本大任务只交付静态演示原型，不实现真实模型调用；harness validate 通过 |
| [x] | M25-T02 | 搭建管理后台智能复核配置页原型 | M25-T01 | 侧边导航新增智能复核入口与独立视图；配置页包含复核总开关、供应商预设（OpenAI、DeepSeek、通义千问、自建中转）、接口地址、模型 ID、密钥掩码输入、每日上限、超时秒数和图像出站授权二次确认；连接测试给出成功、鉴权失败、超时、额度不足四种 Mock 反馈且文案指向要检查的字段；未开启或未授权时页面明确提示不会发送任何图像；密钥仅以掩码呈现，页面、脚本与 Mock 数据中不出现明文密钥；node --check app/static/app.js 通过 |
| [x] | M25-T03 | 在告警列表与详情呈现复核结论 | M25-T01 | 告警列表新增复核结论标记；告警详情新增复核区块展示结论、理由、模型 ID、提示词版本、耗时与来源标记（模型复核或人工复核），未复核、复核失败与已由人工确认三种状态有区分文案；区块内显式说明复核结论只标注不删除告警；提供人工确认误报的 Mock 操作并写入该告警的复核来源；既有告警处置、证据图片与报警推送演示不回归 |
| [x] | M25-T04 | 搭建复核用量与效果看板原型 | M25-T02, M25-T03 | 数据看板新增复核用量区块：今日调用次数与每日上限、结论分布（确认、疑似误报、无法判定）、失败原因分布（鉴权、限流、超时、额度不足）以及复核前后误报率对比；数值来自统一 Mock 数据源，今日与近 7 天、近 30 天切换后保持自洽，且与配置页的每日上限一致；新增 tests/test_admin_verification.py 覆盖导航、配置字段、结论展示与用量区块，与既有 tests/test_admin_*.py 一并通过 |
| [x] | M25-T05 | 修复 M25 引入的智能复核与数据看板样式回归 | M25-T04 | 修复智能复核配置页、告警中心复核按钮与数据看板大屏一屏布局；覆盖桌面多分辨率与移动端；node --check app/static/app.js、管理后台回归测试、harness validate 和 render 通过 |
| [x] | M25-T06 | 补齐智能复核控件与告警中心按钮布局 | M25-T05 | 补齐智能复核全部表单控件、开关、状态提示和按钮样式；修复告警详情确认告警/标记误报按钮位置、间距和响应式布局；覆盖桌面与移动端；node --check app/static/app.js、管理后台回归测试、harness validate 和 render 通过 |
| [x] | M25-T07 | 修复告警中心列表复核操作列对齐 | M25-T06 | 告警中心桌面与移动端列表中，复核状态和箭头固定在统一操作列；表头、行内容和响应式布局列数一致；浏览器实测不出现换行错位；node --check、管理后台回归测试、Harness validate/render 通过 |
| [x] | M25-T08 | 接入告警主动发起 AI 复核接口与后台入口 | M25-T07 | 新增 POST/GET /api/alerts/{event_id}/verification；手动触发受管理员权限、智能复核开关、图像出站授权、每日额度和单事件去重约束；未配置供应商时返回可诊断失败但保留原始告警；PC 告警列表和详情提供发起智能复核按钮并展示 PENDING/FAILED/已复核状态；不覆盖告警处置状态；接口、前端脚本与管理后台回归测试通过 |

## [x] M26 本机模型上传与异步 WSL 转换

大任务状态：`completed`  
依赖：M06  
验收文档：`harness/acceptance/M26-local-model-conversion.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M26-T01 | 实现转换配置与隔离执行器 | 无 | 本机 WSL/Python 配置持久化，环境检测与固定脚本调用使用参数数组，串行持久化任务覆盖超时、失败、重试和重启中断；自动化测试通过。 |
| [x] | M26-T02 | 接通 PT 上传校验与双端产物登记 | M26-T01 | 管理员上传 PT 后真实试推理再登记服务端产物；独立转换 TFLite、校验张量并登记 Android 产物；保留原始标签与版本，移动端失败不影响服务端可用状态；接口与异常测试通过。 |
| [x] | M26-T03 | 接通后台转换配置与任务交互 | M26-T02 | 后台模型页提供真实环境配置、环境检测、上传进度、转换任务状态和重试，Live 操作不使用 Mock 数据，文档明确 COCO 测试定位与 App 接线边界；脚本与页面验证通过。 |
| [x] | M26-T04 | 完成本机真实转换与功能回归 | M26-T03 | 复用已安装 WSL 虚拟环境，以 COCO 执行真实上传、PT 试推理、TFLite 转换与验证、目录下载哈希核对及服务端绑定；记录实际结果与限制，不宣称自定义业务精度或 App 安装通过。 |

## [x] M27 远程模型转换服务适配

大任务状态：`completed`  
依赖：M26  
验收文档：`harness/acceptance/M27-remote-model-conversion.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M27-T01 | 冻结远程转换 HTTP 协议与安全配置 | 无 | 定义提交、查询、取消与产物下载协议，冻结幂等键、Bearer 鉴权、HTTPS 门禁、超时和错误映射；配置序列化不持久化明文令牌 |
| [x] | M27-T02 | 实现远程转换执行器与本地复验 | M27-T01 | remote 执行器完成 PT 上传、状态轮询、取消和产物下载；支持超时、重试与重启恢复；产物经本地哈希和 TFLite 契约复验后才允许登记，自动化测试覆盖异常分支 |
| [x] | M27-T03 | 接通后台远程配置与回归测试 | M27-T02 | 后台可选择 remote、配置 endpoint 与令牌环境变量名并检测连通性；不会回显凭据；local/WSL/remote 回归测试和运维文档通过 |

## [~] M28 管理后台模型资产去 Mock 化

大任务状态：`in_progress`  
依赖：M27  
验收文档：`harness/acceptance/M28-real-model-assets.md`

| 状态 | ID | 小任务 | 依赖 | 完成标准 |
|---|---|---|---|---|
| [x] | M28-T01 | 将模型资产视图固定接入真实目录 | 无 | 移除模型资产视图对 MOCK.models 的展示回退；进入模型资产页和刷新按钮读取 /api/models，空目录和失败状态可见，其他演示视图保持不变 |
| [x] | M28-T02 | 移除模型资产页旧的手机配置读取入口 | 无 | 模型资产页不再显示连接并读取配置按钮；模型目录仍自动读取真实 API，转换配置与真实上传转换流程不受影响；脚本检查和管理后台模型回归测试通过。 |

