---
task_id: M12-T01
major_task_id: M12
status: completed
owner: codex
started_at: 2026-09-03T15:14:23+08:00
completed_at: 2026-09-03T15:28:41+08:00
---

# M12-T01 搭建管理后台产品原型与 Mock 数据视图

## 完成摘要

完成带侧边导航的管理后台产品原型，新增总览、视频流、告警中心、规则、模型和系统诊断视图；默认使用内置 Mock 数据并保留 Live API 接入边界。

## 完成标准

重构 /admin 为带侧边导航的工业巡检管理台，覆盖总览、视频流、告警中心、规则配置、模型资产与系统诊断；内置 Mock 数据和原型交互可独立运行；不要求接通真实 API，但保留后续接入边界并通过静态页面与服务端回归测试

## 测试结果

- node --check app/static/app.js 通过
- python -m unittest tests.test_model_conversion_regression 通过（2项）
- python -m unittest discover -s harness/tests -p test_*.py：24项通过，4项因Windows TEMP权限错误失败

## 证据

- app/static/index.html
- app/static/styles.css
- app/static/app.js
- start-server.ps1（增加 UTF-8 BOM，兼容 Windows PowerShell 5.1 启动）
- app/static/styles.css（桌面端固定侧栏、右侧内容独立滚动）
- harness/acceptance/M12-admin-prototype.md

## 遗留问题

- 当前 Python 环境未安装 `fastapi` 与 `cv2`，无法在本机执行依赖真实服务运行时的完整测试集；页面原型本身已通过 Node 语法检查和模型转换静态回归检查。
- Harness 的 4 个临时目录测试受 Windows TEMP ACL 拒绝访问影响，属于测试环境问题，需在权限正常的环境重跑。
- Mock 数据、规则按钮和 Live API 切换仍是原型占位，不代表真实告警、流媒体和模型业务已接通。
