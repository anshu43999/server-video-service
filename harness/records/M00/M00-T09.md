---
task_id: M00-T09
major_task_id: M00
status: completed
owner: codex
started_at: 2026-09-20T11:26:04+08:00
completed_at: 2026-09-20T11:31:45+08:00
---

# M00-T09 统一跨 Linux Docker Compose 部署

## 完成摘要

将旧开发 docker-compose.yml 与 compose.centos.yml 合并为唯一 compose.yml；统一根目录 .env.example 和 deploy/deploy.sh；保留 Dockerfile 与 Dockerfile.converter 分别构建后端和转换服务；CentOS/RHEL 仅保留 Docker、firewalld、SELinux 主机说明；同步更新部署文档、MediaMTX 文档、远程转换文档和测试。

## 完成标准

项目只保留一套标准 compose.yml 编排 video-service、model-converter、PostgreSQL 与 MediaMTX；根目录提供统一 .env.example，deploy/deploy.sh 作为通用部署入口；CentOS/RHEL 差异仅保留在宿主机说明中；旧 compose.centos.yml、docker-compose.yml 与 deploy/centos 部署文件不再作为独立编排；自动化测试和文档引用全部更新

## 测试结果

- test_docker_deployment 9/9；test_mediamtx_deployment 4/4；test_converter_service 5/5；test_remote_conversion 4/4；test_conversion 13/13；compose.yml 四服务 YAML 解析通过；Git Bash deploy/deploy.sh 语法检查通过；本机无 Docker CLI，真实镜像构建留待 Linux 服务器验证。

## 证据

- compose.yml; .env.example; deploy/deploy.sh; deploy/centos/README.md; docs/docker-deployment.md; tests/test_docker_deployment.py

## 遗留问题

- 无；如有遗留问题，必须在此处明确记录并创建后续任务。
