---
task_id: M28-T07
major_task_id: M28
status: completed
owner: codex
started_at: 2026-09-20T16:54:56+08:00
completed_at: 2026-09-20T17:45:16+08:00
---

# M28-T07 实现校准集资产管理与按任务选择

## 完成摘要

实现版本化校准集资产管理：后台支持 ZIP 上传、列表、选择和引用保护删除；服务端生成受控 YAML 与内容哈希并冻结转换快照；模型目录暴露校准集审计信息；Compose 使用独立 calibration-data 持久卷供转换器只读消费。

## 完成标准

管理后台新增校准集资产列表、ZIP 上传、详情和安全删除；后端持久化名称、版本、场景、图片数、大小、YAML 与内容哈希，并校验图片格式、路径穿越和解压资源上限；提供仅供开发功能验证的 coco8-dev 档案；移动端 INT8 转换必须按校准集 ID 选择并冻结版本与哈希，远程转换器只解析受控校准资产路径；被模型或转换任务引用的校准集版本不得删除；校准图片不进入 Git 并由 Docker 持久卷保存；API、数据库、管理后台、转换和部署回归测试通过

## 测试结果

- 任务相关 48 项单元测试通过；Ultralytics 8.4.138 成功解析生成的 dataset.yaml；Alembic head/offline SQL、compileall、node --check、git diff --check、Harness 30 项测试均通过；Docker 部署测试 11 项通过，seed registry 用例的 3 个错误来自用户现有 models/registry.json 引用未纳入 HEAD 的本地文件；全量 461 项中其余错误主要来自本机 PostgreSQL 尚未执行本次迁移及现有模型注册表脏数据。

## 证据

- app/calibration.py; app/conversion_api.py; app/static/conversion.js; alembic/versions/20260920_05_calibration_datasets.py; compose.yml; docs/local-model-conversion.md; tests/test_conversion_api.py; tests/test_admin_calibration_assets.py；实际浏览器验收桌面与 390x844 移动视口无重叠。

## 遗留问题

- 无；如有遗留问题，必须在此处明确记录并创建后续任务。
