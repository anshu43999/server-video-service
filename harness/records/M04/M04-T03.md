---
task_id: M04-T03
major_task_id: M04
status: completed
owner: codex
started_at: 2026-09-04T17:29:00+08:00
completed_at: 2026-09-04T17:34:24+08:00
---

# M04-T03 完成多流并发与容量压测

## 完成摘要

新增可重复的多流容量压测基元与命令行工具。生产模式并发调用 MediaMTXPublisher.publish，确保每帧先走 H.264 编码再推送；dry-run 仅作 H.264 编码开销校准，不得替代真实 MediaMTX 容量结论。结果输出请求/成功帧数、有效 FPS、编码 P50/P95、每路状态及 CPU/内存采样，并在文档中冻结容量判定门槛、降级顺序和 N+1 扩容指标。

## 完成标准

在包含 H.264 编码与推流的完整链路上明确单机并发流数量、资源上限、降级策略和扩容指标；编码开销单独计量，M02-T03 的无编码容量数字不得沿用

## 测试结果

- .venv\\Scripts\\python.exe -m unittest discover -s tests -p 'test_capacity.py' -v (4 passed)
- .venv\\Scripts\\python.exe -m unittest discover -s tests -p 'test_encoder.py' -v (5 passed)
- .venv\\Scripts\\python.exe -m unittest discover -s tests -p 'test_publisher.py' -v (4 passed)
- .venv\\Scripts\\python.exe -m unittest discover -s tests -p 'test_*.py' (221 tests, 1 skipped; existing MediaMTX compose assertion mismatch)
- .venv\\Scripts\\python.exe -m unittest discover -s harness/tests -p 'test_*.py' (28 tests, 4 Windows Temp permission errors)
- python harness/harness.py validate (passed)

## 证据

- app/capacity.py
- tools/capacity_benchmark.py
- tests/test_capacity.py
- docs/capacity-benchmark.md

## 遗留问题

- 无；如有遗留问题，必须在此处明确记录并创建后续任务。
