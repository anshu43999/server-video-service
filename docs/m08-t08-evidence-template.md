# M08-T08 播放器兼容性实测记录模板

> 仅把真实浏览器/播放器原始输出填入此表。HTTP 探针只能证明端点可访问，不能证明首帧或端到端延迟。

## 环境

- 日期/时区：
- 服务版本/配置：
- MediaMTX 版本：
- 输入源、分辨率、FPS、编码参数：
- 网络条件：

## 浏览器 WHEP（生产主链路）

- 浏览器与版本：
- WHEP URL（不得含令牌）：
- 建连成功次数/失败次数：
- 首帧样本（ms，原始日志路径）：
- 首帧 P50/P95（ms）：
- 端到端延迟样本（ms，时间戳来源）：
- 端到端 P50/P95（ms）：
- 重连次数：
- 结论：`passed` / `blocked` / `failed`

## LL-HLS 回退

- 触发方式（WHEP 非 2xx、UDP 阻断等）：
- 清单与分片请求结果：
- 首帧与端到端延迟（如有）：
- UI/日志是否明确显示“回退”：
- 结论：`passed` / `blocked` / `failed`

## RTSP 诊断

- 工具与版本（VLC/ffprobe）：
- RTSP URL（不得含令牌）：
- 解码连续画面：
- 首帧时间（如工具可测）：
- 结论：`passed` / `blocked` / `failed`

## Android（如设备可用）

- 设备型号/API/ABI：
- WebRTC 或播放器库版本：
- WHEP 主链路结果：
- LL-HLS 回退结果：
- 首帧、P50/P95、重连与 30 分钟稳定性：

## 冻结项

缺少设备、浏览器、MediaMTX、网络注入权限或原始日志时，填写阻塞原因和解除条件；不得以脚本启动、HTTP 200 或“地址已生成”作为播放通过证据。
